import { FilterOption } from '@/types/filters';

export const HEADER_HEIGHT = 60;

export const FIRST_PAGE_NUMBER = 1;
export const PAGE_SIZE = 10;

export const FILTER_OPTIONS: FilterOption[] = ['all', 'female', 'male', 'n/a'];
